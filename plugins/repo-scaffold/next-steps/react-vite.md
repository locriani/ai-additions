  - cd "$REPO"
  - pnpm create vite@latest . --template react-ts   (forces in-dir; will
    prompt about overwriting — say no for files the scaffold owns)
  - pnpm install
  - Edit CLAUDE.md: confirm framework choice (Vite-only vs Next.js App Router).
    The stack profile covers both.
  - Optional: /init-ai-memory in the repo root.
