# <<NAME>>

<<one-line — what this is>>

## Why

<<why this exists — the problem it solves or the reason it's a separate repo>>

## Quick start

```bash
# Clone
git clone <repo-url> <<NAME>>
cd <<NAME>>

# Install
<<install command — e.g. uv sync, pnpm install, bundle install>>

# Run
<<run command>>
```

## Usage

<<common workflows; show real commands, not placeholders the reader has to guess at>>

## Architecture

<<how it's organized — only as much as a contributor needs. Delete this section if there's nothing non-obvious to say.>>

Stack: **<<STACK>>** (see [`CLAUDE.md`](CLAUDE.md) for project conventions and [`~/.claude/stacks/<<STACK>>.md`](~/.claude/stacks/<<STACK>>.md) for the stack profile).

## Development

```bash
# Tests
<<test command>>

# Lint / type-check
<<lint command>>

# Build
<<build command>>
```

Project conventions live in [`CLAUDE.md`](CLAUDE.md).

## License

<<MIT / Apache-2.0 / proprietary / TODO>>
